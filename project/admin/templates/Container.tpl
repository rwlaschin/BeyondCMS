<div class='bcms-container' ng-controller="bcms-{{name}}">
    <script> 
    // title : visible hidden (visible if menu item)
    // size
    // position

        // returns object
        $bcms.initializeWidget( function(container) {
            
        } );
    </script>
</div>