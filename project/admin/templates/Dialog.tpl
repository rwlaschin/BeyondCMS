<div class='bcms-view bcms-dialog' ng-controller="bcms-{{name}}">
    <script>
    // title
    // size
    // 'position is centered'
    // message (multi-line)
    // buttons OK, Cancel, Yes, No

        // returns object
        $bcms.initializeWidget( function(container) {
            
        } );
    </script>
</div>