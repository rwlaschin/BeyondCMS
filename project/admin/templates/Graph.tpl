<div class='bcms-view bcms-graph' ng-controller="bcms-{{name}}">
    <script> 
    // Depends on graph type
    // Title
    // size
    // position
    // filter (this will return the filter data)
        // returns object
        $bcms.initializeWidget( function(container) {
            
        } );
    </script>
</div>